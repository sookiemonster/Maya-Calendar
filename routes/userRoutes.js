// Require necessary modules
const express = require("express");
const router = express.Router();

// Login, register, update details, logout

// Export the routes for server handling
module.exports = router;